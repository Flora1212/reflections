cd ~                          # change directories to your home directory
mkdir version-control         # make version-control directory
cd version-control            # go to version-control directory
mkdir reflections             # create reflections directory
cd reflections                # go to reflections directory
atom lesson_1_reflections.txt # launch atom with file called lesson_1_reflections.txt

pwd # print working directory - shows what directory you are in
ls  # list the files in this directory


git clone   # clone a repository
git log     # check commit history
git checkout   # set temporarily to a history version - can set back by checkout the most recent commit
git diff    # check the difference between two commits
